import { MainContainer, SectionContainer } from '../../../styles'

const Loading = () => {
  return (
    <MainContainer>
      <SectionContainer>
        <p>Loading...</p>
      </SectionContainer>
    </MainContainer>
  )
}

export default Loading
